export const metadata = {
  title: "Medical Kits",
};

const MedkitsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
};

export default MedkitsLayout;
