export const metadata = {
  title: "Transactions | Medical Kits",
};

const TransactionLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
};

export default TransactionLayout;
